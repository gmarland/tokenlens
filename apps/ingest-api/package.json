{
  "name": "@tokenlens/ingest-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --tsconfig ../../tsconfig.json src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "tsx --tsconfig ../../tsconfig.json src/server.ts",
    "test": "vitest run --root ../.. apps/ingest-api/src"
  },
  "dependencies": {
    "@tokenlens/database": "workspace:*",
    "@tokenlens/otel-parser": "workspace:*",
    "@tokenlens/shared": "workspace:*",
    "dotenv": "^17.0.0",
    "fastify": "^5.0.0",
    "tsx": "4.20.5",
    "zod": "4.1.5"
  }
}
